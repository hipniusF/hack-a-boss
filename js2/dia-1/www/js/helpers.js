const add = (a1, a2) => a1 + a2;
const substraction = (a1, a2) => a1 - a2;
const RADIO_TIERRA = 6397;

export function multiple(a, b) {
	return a * b;
}

export default add;
export { RADIO_TIERRA, substraction };
